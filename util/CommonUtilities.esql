BROKER SCHEMA util

CREATE PROCEDURE toUpperCase(IN name CHAR) 
RETURNS CHAR BEGIN
	RETURN UCASE(name); 
END;

CREATE PROCEDURE toLowerCase(IN name CHAR) 
RETURNS CHAR BEGIN
	RETURN LCASE(name); 
END;

CREATE PROCEDURE getLength(IN name CHAR) 
RETURNS INT BEGIN
	RETURN LENGTH(name); 
END;
